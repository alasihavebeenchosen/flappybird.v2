import greenfoot.*;  // (World, Actor, GreenfootImage, Greenfoot and MouseInfo)
import java.util.List; 

public class Pipe extends SmoothMover
{
    int pipeSpeed = -4; 
    
    public void act()
    {
      setLocation( getX() + pipeSpeed, getY()+image.getHeight()/2 );
    }
      
  }
}
